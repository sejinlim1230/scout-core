# Scout Core 작업 지침

이 저장소는 Excel 등으로 관리하던 업무를 웹 기반 운영 도구로 전환할 때 사용하는 재사용 가능한 기본 뼈대다. 새 프로젝트에서는 Scout Core를 직접 덮어쓰지 말고 별도 저장소로 복제한 뒤 도메인에 맞게 수정한다.

## 제품 목표

- 사용자가 중요한 업무를 빠르게 파악하고 즉시 처리할 수 있게 한다.
- 대시보드, 파이프라인, 목록, 상세, 후속 액션, 분석 구조를 재사용한다.
- 고객의 용어, 데이터 구조, 권한, 업무 흐름에 맞춰 모듈을 교체한다.
- 실제 운영 전까지 익명화된 샘플 데이터로 개발하고 검증한다.

## 작업 시작 전

1. Notion의 `신규 툴 제작 요청서 — Scout Core 기술서 템플릿`과 첨부 자료를 읽는다.
2. 프로젝트 목적, 핵심 사용자, 필수 기능 3가지, 완료 기준을 요약한다.
3. Excel의 시트, 열, 고유키, 관계, 수식, 중복 기준, 빈칸의 의미를 확인한다.
4. 요구사항에서 확정된 내용, 합리적인 가정, 사용자 확인이 필요한 내용을 구분한다.
5. Scout Core에서 재사용할 모듈과 새로 만들거나 교체할 모듈을 명시한다.

기술서: https://app.notion.com/p/3a583171e9fa811fabe3caf8763ed189

## 데이터 안전

- 실제 개인정보, 고객 운영 데이터, 비밀번호, API 키, 토큰을 저장소에 커밋하지 않는다.
- 개발에는 익명화된 샘플 데이터를 사용한다.
- 환경별 비밀값은 환경 변수로 분리하고 `.env` 파일은 추적하지 않는다.
- 공개 저장소에는 실제 데이터, 데이터베이스 덤프, 사용자 파일을 올리지 않는다.
- 삭제, 일괄 수정, Excel 가져오기에는 미리보기와 명시적 확인 단계를 둔다.
- 운영 전 사용자별 조회·등록·수정·삭제·다운로드 권한을 검증한다.
- 운영 전 백업 생성과 복구 절차를 실제로 시험한다.

## UX 및 UI 원칙

- 첫 화면에는 가장 중요한 지표와 당장 처리할 업무를 우선 표시한다.
- 동일한 의미의 KPI를 중복 배치하지 않는다.
- KPI마다 기간, 중복 제거, 테스트 데이터 제외, 계산 기준을 문서화한다.
- 로딩 실패를 `0건`이나 빈 목록으로 표시하지 않는다.
- 로딩, 오류, 빈 상태, 저장 중, 성공 상태를 각각 분명히 제공한다.
- 저장 중에는 중복 클릭을 차단하고 성공·실패 결과를 즉시 안내한다.
- 검색, 필터, 정렬 상태가 사용자가 보는 목록과 Excel 내보내기 결과에 일치해야 한다.
- 위험한 작업은 결과와 영향 범위를 먼저 보여주고 확인을 받는다.
- 키보드 접근성, 명확한 포커스, 읽기 쉬운 대비, 모바일 사용성을 유지한다.
- 사용자 입력을 검증 없이 `innerHTML`에 삽입하지 않는다.

## Excel 마이그레이션과 내보내기

- 원본 Excel을 변경하지 않고 보존한다.
- 가져오기 전에 열 매핑, 필수값, 날짜·금액 형식, 중복 기준을 확인한다.
- 잘못된 행은 조용히 버리지 말고 행 번호와 오류 원인을 제공한다.
- 가져오기 전후의 전체 행 수, 고유키 수, 상태별 수, 핵심 KPI를 비교한다.
- 전체 데이터, 현재 검색·필터 결과, 선택 행 중 요구된 범위를 내보낼 수 있게 한다.
- 다운로드 권한과 개인정보 마스킹 규칙을 적용한다.
- Excel 시트명, 열 순서, 날짜·금액 형식, 파일명 규칙을 기술서와 일치시킨다.

## 코드 구조

- `config.js`: 프로젝트명, 메뉴, 상태, 표시 문구 등 프로젝트별 설정
- `data.js`: 데모 데이터와 데이터 형태 예시
- `app.js`: 화면 상태와 사용자 상호작용
- `export.js`: Excel 워크북과 다운로드 생성
- `styles.css`: 공통 UI 스타일
- `export.css`: 내보내기 화면 스타일
- `MIGRATION-CHECKLIST.md`: 데이터·계정·운영 전환 점검표

도메인 설정, 데이터 접근, 화면 렌더링, 파일 내보내기를 가능한 한 분리한다. 새 기능을 추가할 때 한 파일에 모든 책임을 집중시키지 않는다.

## 구현 방식

- 기존 사용자 변경사항과 관련 없는 코드를 되돌리지 않는다.
- 요청 범위 안에서 가장 작은 변경으로 완성한다.
- 임시 데모 동작과 실제 저장 동작을 혼동하지 않도록 화면과 문서에 표시한다.
- 외부 라이브러리는 필요한 경우에만 추가하고 사용 목적과 의존성을 README에 기록한다.
- 네트워크나 외부 CDN 실패가 핵심 기능에 미치는 영향을 검토한다.
- 기능을 완료하면 관련 README와 마이그레이션 문서를 함께 갱신한다.

## 검증 기준

변경 후 최소한 다음을 확인한다.

1. JavaScript 문법 오류가 없는지 검사한다.
2. 주요 화면을 브라우저에서 직접 연다.
3. 콘솔 오류와 경고를 확인한다.
4. 로딩, 오류, 빈 상태, 정상 데이터 상태를 확인한다.
5. 등록·수정·저장 중 중복 클릭 방지를 확인한다.
6. 검색·필터·정렬과 Excel 결과의 일치 여부를 확인한다.
7. 모바일 크기와 키보드 이동을 확인한다.
8. 기존 Excel과 행 수, 상태별 수, 핵심 KPI를 비교한다.

테스트하지 못한 항목은 완료로 표현하지 말고 이유와 남은 위험을 보고한다.

## Git과 배포

- Scout Core 원본과 고객별 프로젝트 저장소를 분리한다.
- 커밋 작성자는 개인 계정 `sejinlim1230@gmail.com`을 사용한다.
- 커밋은 하나의 목적을 설명하는 짧고 명확한 메시지로 남긴다.
- 사용자의 요청 없이 회사 저장소나 운영 환경에 직접 배포하지 않는다.
- 운영본은 최종 사용자가 소유한 GitHub, 데이터베이스, 호스팅 계정으로 이전한다.
- 공개에서 비공개로 전환하는 것만 개인정보 보호 수단으로 간주하지 않는다. 커밋 기록에 비밀값이나 실제 데이터가 있었는지도 확인한다.
- 배포 후 공개 URL에서 핵심 흐름과 다운로드를 다시 검증한다.

## 완료 보고

작업을 마칠 때 다음을 간단히 보고한다.

- 무엇을 만들거나 변경했는지
- 사용자가 어디에서 확인할 수 있는지
- 수행한 검증과 결과
- 남은 가정, 위험, 사용자 확인 사항
- 다음으로 가장 안전한 작업
